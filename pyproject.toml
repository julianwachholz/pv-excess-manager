[project]
name = "pv-excess-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["colorlog>=6.10.1", "homeassistant>=2026.2.3", "ruff>=0.15.6"]
